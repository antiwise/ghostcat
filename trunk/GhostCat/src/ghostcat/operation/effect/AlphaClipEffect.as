package ghostcat.operation.effect
{
	public class AlphaClipEffect extends TweenEffect
	{
		public function AlphaClipEffect(target:*, duration:int, params:Object, invert:Boolean=false)
		{
			super(target, duration, params, invert);
		}
	}
}