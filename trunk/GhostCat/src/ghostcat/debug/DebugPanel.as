package ghostcat.debug
{
	import ghostcat.ui.containers.GResizePanel;
	
	public class DebugPanel extends GResizePanel
	{
		public function DebugPanel(skin:*=null, replace:Boolean=true)
		{
			super(skin, replace);
		}
	}
}