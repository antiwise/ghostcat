类库未完成。不提供DownLoad，请自行从SVN上拉。
如果有错误请尽快提出，我会马上修改。

@author flashyiyi
blog:http://uh.actionscript3.cn/space.php?uid=12147

----------------------------------------------------------
Main Function Points 

Tool sets and frameworks 

It provides a miniature visual object framework, similar to FLEX delayed update mechanism to achieve some basic optimization of processing and infrastructure functions. This will provide a lot of new features to facilitate the development. 
Provides a wealth of practical classes, to cover the color matrix conversion, bitmap caching, text processing, reflecting management, filter agents, fonts, events, mouse, sound, drag, the difficulty for many developing solutions to provide a standard . 
Compared with the general framework of its various functions and the framework itself has lower coupling. Is therefore more similar to the tool set. 

Bitmap animation and vector animation centrally 

GMovieClipBase is an animated abstract classes in order to frame label processing unit can be easily spliced loop animation, a simple treatment of complex animations, especially for walking off the direction of turn eight and so a coherent movement. 
The game must be to achieve a constant rate (jump) function. 
Such as the abstract class, and its two entities, classes, GMovieClip, GBitmapMovieClip were realized on the common vector and bitmap animation array of animation centrally. 

Pan-use of the dynamic display module 

Tile class will be automatically tiled in one element of the dynamic of the deletions within the region to simulate the large range of repeated primitives. You can use him as a map grid, can also be used to do some shows need to repeat the background of the picture. 
It uses elements of the incident update, use the intuitive, convenient, and easier to extend, or even can be used to achieve components List. 
It derived class Tile45 realize the game is commonly used in the game map features a 45-degree angle. 

The sequence data using XML serialization and anti - 

Similar to the FLEX tag representation to achieve the basic function (attribute, ID binding, events). On the design purposes, and not only to that interface, the same can also be used with other data to deserialize (such as oper definition) 
Designed with an open-style design, you can switch the parser to deal with a variety of different situations, and can modify and redesign their own parser.

To the queue as a means to simplify asynchronous approach 

Queue system easy to use, it is easy to extend. It can be load of resources, methods, implementation of mixed-queue model, in order to achieve a similar approach to synchronize the original. Can even determine which conditions of accession and circulation. 
Rely on it, and was built to FilterProxy queuing Tween playback and support for filters, narrowed and TweenMax functional distance. 
Rely on it to achieve a simple and flexible QUEST system that can help achieve the tutorial, RPG nature of the effect of the plot shows

To the skin as the main GUI class library

This is a free addChild to any visual container on the UI component library, there is no precise notion of the container, all the components only need to addChild to stage to take effect. 
In meeting the above requirements at the same time to optimize the functional. Since the main for the game, right MovieClip good support, adaptive content is more inclined than the expression and diversity. This is not the main data. 
Components tend to replace the original pixel on the graphics to achieve the components of the skin, not to make interface and then attached to the skin, but rather directly into the skin interface, thus enabling a perfect graphic design to show the contents of the script. But not exclusive way the original skin. 
Please note. Use of the main points of this library is "art first." "Program first" and even "no art", then do not apply.

-------------------------------------------------- -------- 

A number of miscellaneous functions 

Collision 

An alternative collision detection module. Using a graphical expression of the "circular" and "rectangular" as the collision of the standards in order to splice out these basic graphical objects irregular shape, which replaces the slower way to judge the bitmap. 

A * 

Common types of A * algorithm. Speed has been slow, but the expansion of the performance very good, very easy to read. 

Roupe 

This is an interesting example of the application of Bell's Law curve. One has the physical properties of the elastic cord. 
You can refer to the curve of its trajectory to achieve TweenMax function. 

TweenUtil 

And TweenLite similar slow moving module, for certain categories of library use (such as SoundManager). It does not TweenLite of high efficiency, but also not far. Functions to meet the Queue, and most of the demand can be achieved FilterProxy. 

SWFDecoder 

Although far less functional SWFAssert, but also can achieve a lot of demand for the. Because of the its main purpose is to write your own GhostCatTool, TAG not much. We can refer to swf_file_format_spec_v9.pdf to write their own needs TAG, casual look to find a TAG should be written on the know how to do. 

IME 

The internal FLASH Pinyin input method, need to load the thesaurus

tranfer 

This package is mainly used to deal with some need to be converted into a bitmap and then to achieve things. For example thumbnails, reflection, trapezoidal transformation. 

parse 

This package is used to convert an object operation. In particular, easy to use for the Graphics. Primarily for other types of support. However, this approach is very useful. 

Drag Tool 

display.graphics offers a range of Graphic editing tools can help achieve the foundation classes. (Graphic refers to the flow chart similar to that kind of stuff) 

Single-file self-loading 

Frame Meta tags used to achieve single-file self-loading, this should be a very common need. 

Mouse gestures 

InputManager joined in the way I used to write mouse gesture system. This increases the game's user experience is very useful. 
Monitor events and used PathAnalyzer analysis on it. 

Shadow Builder

Can generate an object's shadow, and shadow can also be folded into the wall

----------------------------------------------------------
Name of Source: 

Each monsters, have all served as human beings. 
- This is only circulated among monsters, preposterous urban legend. 

"Why do monsters eat people?" 
"As for the monsters, the humans are engraved in their hearts they are the most perfect existence. Therefore, even if the possibility of only a little thin, even if the end would therefore encounter, they still could not suppress this kind hearts desire - to human existence one step closer to the desire to eventually lose their reason. " 

Memory disappears. Hypothetical construct of the self, or debris, or origin? 
Lazy filled hollow heart, body hair licked back of the hand. 
Moonlit Traceless, ghost cat.

-----------------------------------------------------------------------------

主要功能点


工具集以及框架

它提供了一个微型的可视对象framework，采用类似FLEX的延迟更新机制，实现了一些基本的优化处理以及基础功能。这将会为新功能的开发提供不少便利。
提供了大量实用类，涵盖到颜色，矩阵转换，位图缓存，文本处理，反射管理，滤镜代理，字体，事件，鼠标，声音，拖拽，为许多开发中的难点提供了标准解决方案。
与一般的框架相比，它的各项功能和框架本身的耦合度较低。因此更类似于工具集。

位图动画和矢量动画的统一处理

GMovieClipBase是对动画处理的抽象类，以帧标签为处理单位，可以方便地将动画拼接循环播放，实现复杂动画的简单处理，尤其适用于八方向行走起步转身等连贯动作。
实现了游戏必须的恒定速率（跳帧）功能。
此类为抽象类，它的两个实体类，GMovieClip，GBitmapMovieClip则分别实现了对普通矢量动画以及位图数组动画的统一处理。

泛用化的动态显示模块

Tile类会自动将元素平铺在一个区域内进行动态的增删，以模拟超大范围的重复图元。您可以将他作为地图格子使用，也可以用做一些需要进行不断重复显示的背景图片上。
它采用事件进行元素的更新，使用直观便利，且较易进行扩展，甚至可以用来实现组件List。
它的派生类Tile45则实现了游戏常用的45度角游戏地图功能。

采用XML进行数据的序列及反序列化

类似FLEX的标签表示形式，实现了基本的功能（属性设置，ID绑定，事件）。就设计目的而言并不仅仅是为了表示界面，同样也可以用与其他数据的反序列化（诸如oper定义）
采用的是开放式的设计，可以切换解析器来应对多种不同的情况，并可自行修改和重新设计解析器。

以队列为手段的简化异步处理方式

简单易用的队列系统，很容易进行扩展。它可以实现资源加载，方法执行的混合队列模式，以实现类似原来的同步的处理方式。甚至可以在里面加入条件判断和循环。
依赖它和FilterProxy实现了内置Tween的排队播放和对滤镜的支持，拉近了和TweenMax功能上的距离。
依赖它实现了一个简单灵活的QUEST系统，可以帮助实现教程，RPG性质的情节演出效果

以皮肤为主体的GUI类库

这是一个可以自由addChild到任何可视容器上的UI组件库，并没有确切的容器的概念，所有组件只需要addChild到舞台上即可生效。
在满足以上要求的同时尽可能完善了功能。由于主要面向游戏，对MovieClip有良好的支持，比起自适应内容更倾向与多样化的表现方式。数据在这里并不是主体。
组件倾向于替换原有图元上的图形来实现组件皮肤，并不是做出界面然后附上皮肤，而是将皮肤直接转换成界面，因此能够完美呈现美工设计稿上的内容。但也不排斥原有的皮肤方式。
请注意。这个类库的使用要点是“美工先行”。“程序先行”甚至“没有美工”的话，并不适用。


----------------------------------------------------------

一些杂项功能

Collision

另类的碰撞检测模块。使用图形表达的“圆形”和“矩形”作为碰撞的标准，以这些基本图形来拼接出物品不规则的外型，以此代替速度较慢的位图判断方式。

A*

通用型的A*算法。速度虽然较慢，但扩展性能很好，很容易阅读。

Roupe

这是一个有趣的贝尔法曲线的应用实例。一根具有物理特性的弹性细绳。
你可以参考它实现TweenMax的曲线轨迹功能。

TweenUtil

和TweenLite类似的缓动模块，供库里的某些类使用（比如SoundManager）。它并没有TweenLite的效率高，但也相距不远。功能上，配合Queue和FilterProxy可以实现大部分需求。

SWFDecoder

功能虽远不如SWFAssert，但也能实现不少需求了。由于制作它的目的主要是为了编写自己的GhostCatTool，TAG并不多。大家可以参照swf_file_format_spec_v9.pdf来编写自己需要的TAG，随便找个写好的TAG看看应该就知道怎么做了。

IME

FLASH的内部拼音输入法，需要加载词库

tranfer

这个包主要用于处理一些需要转换成位图再实现的东西。比如缩略图，倒影，梯形变换。

parse

这个包用于将操作转换为对象。对于Graphics尤其好用。主要为其他类提供支持。但这种处理方式是很有用的。

拖拽工具

display.graphics提供了一系列可以帮助实现Graphic编辑工具的基础类。（Graphic指的是类似流程图那样的东西）

单文件自加载

利用Frame元标签实现的单文件自加载，这应该是一个很普遍的需求。

鼠标手势

InputManager内顺便加入了我以前写的鼠标手势系统。这对于增加游戏里的用户体验是很有用的。
监听事件并用PathAnalyzer解析就可以了。

影子生成器

可以生成物体的影子，并且影子还可以折到墙壁上

----------------------------------------------------------
名称来源：

每个妖怪，都曾经身为人类。
——这是只在妖怪之间流传的，荒诞无稽的都市传说。

“知道妖怪为什么要吃人吗？”
“因为对于妖怪来说，人类是他们铭刻在心里的最完美的存在。所以哪怕只有稀薄的一点可能性，哪怕会因此遭遇终结，他们依旧无法抑制内心深处的这种欲望——向人类存在更接近一步的欲望，最终欲火焚身。”

记忆散尽。假想所构筑的自我，或残骸，或本源？
慵懒填充了空洞的内心，舔舐着手背的绒毛。
月夜无痕，幽灵猫。