package org.ghostcat.ui.containers
{
	import flash.display.DisplayObject;
	
	import org.ghostcat.display.GBase;
	
	public class ScrollPanel extends GBase
	{
		public function ScrollPanel(skin:DisplayObject=null, replace:Boolean=true)
		{
			super(skin, replace);
		}
	}
}